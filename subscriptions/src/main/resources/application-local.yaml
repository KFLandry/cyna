spring:
  application:
    name: subscriptions
  datasource:
    url: jdbc:mysql://localhost:3306/cyna_db
    username: cyna_user
    password: cyna_pwd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  #  config:
  #    import: configserver:${URL-CONFIG-SERVER:http://localhost:8888}
  #  profiles:
  #    active: ${PROFILE:local}

  docker:
    compose:
      file: compose.yaml
server:
  port: 8083

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost
    preferIpAddress: false
    instance-id: ${spring.application.name}:${random.value}

logging:
  level:
    root: info
    web: debug
  file:
    name: ./target/cyna-orders.log

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

mailerSend:
  from: zodiaquestar@gmail.com
  support_email: support@test-86org8e8d2ngew13.mlsender.net
  super_admin: zodiaquestar@gmail.com
  token: "${MAILERSEND_TOKEN}"
  templates:
    generic_template: "${MAILERSEND_GENERIC_TEMPLATE}"
    generic_template_with_button: "${MAILERSEND_GENERIC_TEMPLATE_WITH_BUTTON}"
    email_verified: "${MAILERSEND_EMAIL_VERIFIED}"


stripe:
  STRIPE_PUBLISHABLE_KEY: "${STRIPE_PUBLISHABLE_KEY}"
  STRIPE_SECRET_KEY: "${STRIPE_SECRET_KEY}"
  STRIPE_WEBHOOK_SECRET: "${STRIPE_WEBHOOK_SECRET}"
  return_url: "${STRIPE_RETURN_URL}"
  STRIPE_API_VERSION: "${STRIPE_API_VERSION}"
