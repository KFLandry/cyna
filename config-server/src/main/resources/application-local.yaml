spring :
  application :
    name : config-server

  cloud:
    config:
      server:
        git:
          uri: file://C:/Projets/Personnel/Cyna_Projet/config-repo
server:
  port: ${PORT:8888}

logging:
  level:
    web: debug
